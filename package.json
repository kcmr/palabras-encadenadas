{
  "name": "palabras-encadenadas",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm start -w=app",
    "test": "npm test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kcmr/palabras-encadenadas.git"
  },
  "author": "Kus Cámara",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kcmr/palabras-encadenadas/issues"
  },
  "homepage": "https://github.com/kcmr/palabras-encadenadas#readme",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.8.2",
    "@parcel/packager-xml": "^2.8.2",
    "prettier": "^2.8.0"
  }
}
